# Bitstreams

No local `.bit`, `.hwh`, `.xsa`, or `.ltx` artifacts were found when this
repository cleanup was performed.

If a generated bitstream is available and the file is small enough for GitHub's
normal file-size limits, place it in this directory with the matching hardware
metadata files and update this README with the Vivado run name, target board,
clock constraint, and generation date.
